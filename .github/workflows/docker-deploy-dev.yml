name: 🚀 DEV Build

on:
  workflow_dispatch:

jobs:
  build:
    uses: brege-ng/infra_devops_pipelines/.github/workflows/build-auto.yml@main
    with:
      dockerfile: ./Dockerfile-dev
      context: .
