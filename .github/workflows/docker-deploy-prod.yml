name: 🚀 PROD Build

on:
  push:
    branches:
      - main
      - release
  workflow_dispatch:

jobs:
  build:
    uses: brege-ng/infra-devops-pipelines/.github/workflows/build-auto.yml@main
    with:
      dockerfile: ./Dockerfile-prod
      context: .
