# ------------------------------
# Stage 1: Builder
# ------------------------------
FROM node:22.14.0-alpine3.20 AS builder
WORKDIR /app

COPY package*.json ./
RUN npm ci  # installe toutes les deps

COPY . .
RUN npm run build

# ------------------------------
# Stage 2: Image finale
# ------------------------------
FROM node:22.14.0-alpine3.20 AS prod
WORKDIR /app

ENV NODE_ENV=production
ENV PORT=3000
ENV HOST=0.0.0.0

# Copier package.json / package-lock.json pour npm ci --omit=dev
COPY package*.json ./
RUN npm ci --omit=dev

# Copier le build depuis le stage builder
COPY --from=builder /app/.next ./.next
COPY --from=builder /app/public ./public
COPY --from=builder /app/next.config.js ./next.config.js

EXPOSE 3000
CMD ["npm", "start"]



#FROM node:22.14.0

#ENV HOST='0.0.0.0'
#ENV PORT='3000'

#WORKDIR /app

#COPY package*.json ./

#RUN npm ci

#COPY . .

#RUN npm run build

#EXPOSE 3000

#CMD [ "npm", "start" ]
